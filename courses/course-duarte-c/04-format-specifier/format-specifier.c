
#include <stdio.h>

int main(void) {

  // FORMAT SPECIFIER
  // Format specifiers are used in input and output functions such as printf and scanf to format variables.
  
  // %d o %i: Para números enteros (int).

  // %u: Para números enteros sin signo (unsigned int).

  // %f: Para números de punto flotante (float o double).

  // %lf: Para números de punto flotante de doble precisión (double).

  // %e o %E: Para números de punto flotante en notación científica.

  // %g o %G: Para números de punto flotante en notación científica o decimal, dependiendo de lo que sea más corto.

  // %c: Para caracteres (char).

  // %s: Para cadenas de caracteres (string).

  // %p: Para punteros (void *).

  // %x o %X: Para números enteros en formato hexadecimal.

  // %o: Para números enteros en formato octal.

  // %%: Para imprimir un signo de porcentaje literal (%).


  return 0;
}
